name: YandexGPT Async AI News Monitor

on:
  schedule:
    - cron: '0 * * * *'  # Каждый час, 24/7
  workflow_dispatch:

jobs:
  publish-hourly-news:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}  # Используем персональный токен вместо GITHUB_TOKEN

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies from requirements.txt
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Debug info
        run: |
          echo "📁 Содержимое папки:"
          ls -la
          echo "📁 Содержимое папки src:"
          ls -la src/
          echo "📄 Содержимое requirements.txt:"
          cat requirements.txt
          echo "🕐 Текущее время UTC: $(date -u)"
          echo "🕐 Текущее время МСК: $(TZ='Europe/Moscow' date)"

      - name: Publish hourly AI News
        env:
          YANDEX_API_KEY: ${{ secrets.YANDEX_API_KEY }}
          YANDEX_FOLDER_ID: ${{ secrets.YANDEX_FOLDER_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
        run: |
          echo "🚀 Запуск монитора в: $(date -u) UTC"
          echo "🕐 МСК время: $(TZ='Europe/Moscow' date)"
          python src/main.py
